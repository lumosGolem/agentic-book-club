# --- Core Orchestration & AI Framework ---
google-adk>=2.0.0
google-genai

# --- Inference & Model Loading (Hugging Face Stack) ---
# Using specific versions to ensure Qwen3-14B architecture support
transformers>=4.45.0
torch>=2.4.0
accelerate>=0.33.0
bitsandbytes>=0.43.3
sentencepiece
tiktoken
einops

# --- Server & Human Interface (IRC server) ---
gradio>=4.40.0
fastapi
uvicorn
pydantic>=2.0.0

# --- Infrastructure & Networking ---
modal>=0.64.0
httpx>=0.27.0
python-dotenv

# --- Optimization ---
# Note: flash-attn often requires a GPU to be present during install
# If install fails on a local machine without a GPU, comment this out.
# flash-attn>=2.6.0
